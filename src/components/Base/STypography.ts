import { Text } from 'react-native';
import { styled } from '../../stitches';

export const SHeadingPrimary = styled(Text, {
  fontSize: '$3xl',
  fontWeight: 'bold',
});
export const SHeadingSecondary = styled(Text, {
  fontSize: '$2xl',
  fontWeight: 'bold',
});

export const SHeadingTertiary = styled(Text, {
  fontSize: '$xl',
  fontWeight: 'bold',
});
export const SFormLabel = styled(Text, {
  fontSize: '$lg',
  fontWeight: 'bold',
  color: '$black',
});
export const SText = styled(Text, {
  fontSize: '$md',
  fontWeight: 'normal',
});
